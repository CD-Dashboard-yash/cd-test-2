name: Night Pipeline

on:
  push:
    branches:
      - main
      - develop
      - release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building..."
      - run: sleep 5
      - run: exit 0
